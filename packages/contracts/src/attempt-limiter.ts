export type HalfOpenWindow={startsAtMs:number;endsAtMs:number}; export interface IngressAttemptLimiter{checkAndRecord(matchId:string,playerId:string,requestId:string,window:HalfOpenWindow,maxAttempts:number):Promise<boolean>}
export class MemoryIngressAttemptLimiter implements IngressAttemptLimiter{private rows=new Map<string,{window:number;requests:Set<string>}>();async checkAndRecord(m:string,p:string,r:string,w:HalfOpenWindow,max:number){const k=`${m}\0${p}`,old=this.rows.get(k);const row=!old||old.window!==w.startsAtMs?{window:w.startsAtMs,requests:new Set<string>()}:old;if(row.requests.has(r))return true;if(row.requests.size>=max)return false;row.requests.add(r);this.rows.set(k,row);return true}}
