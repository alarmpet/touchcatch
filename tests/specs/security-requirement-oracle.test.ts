import fs from'node:fs';import{expect,it}from'vitest';import{evaluateSecurityRequirement,executeRequirementOracle}from'../../tools/requirement-oracle.js';
it('executes exact authenticated delivery, public client, authority and preload predicates',()=>{for(const id of ['SEC-001','SEC-002','SEC-004','SEC-005','SEC-006','SEC-007'])expect(evaluateSecurityRequirement(id)).toBe(true);expect(()=>evaluateSecurityRequirement('SEC-999')).toThrow(/unsupported/);});
it.each(['SEC-001','SEC-002','SEC-004','SEC-005','SEC-006','SEC-007'])('%s binds its exact source row and rejects mutation',(id)=>{const registry=JSON.parse(fs.readFileSync('docs/requirements-registry.v1.json','utf8')),evidence=JSON.parse(fs.readFileSync('config/requirement-evidence.v1.json','utf8')),row=registry.requirements.find((x:{id:string})=>x.id===id),claim=evidence.entries.find((x:{id:string})=>x.id===id);expect(executeRequirementOracle(process.cwd(),row,claim).status).toBe('PASS');expect(executeRequirementOracle(process.cwd(),{...row,text:`${row.text} forged`},claim).status).toBe('FAIL');});
