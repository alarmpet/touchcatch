# Learning image prompt provenance

Each generated A image and controlled B edit stores the exact prompt in this directory before generation. The catalogue keeps SHA-256 fields null until those prompt files and generated assets exist. Human rights and education review remain required before publish.
