import {describe,expect,it} from 'vitest';import {compareRegions,requireApprovedGolden} from './visual-runner.js';
describe('deterministic visual runner',()=>{it('applies deterministic masks and region thresholds',()=>{const golden={width:2,height:1,data:new Uint8Array([0,0,0,255,0,0,0,255])};const actual={...golden,data:new Uint8Array([255,0,0,255,0,0,0,255])};const region={id:'screen',x:0,y:0,width:2,height:1,maxChangedRatio:0,maxChannelDelta:0};expect(compareRegions(actual,golden,[region],[{...region,id:'mask',width:1}])[0]?.pass).toBe(true);expect(compareRegions(actual,golden,[region],[])[0]?.pass).toBe(false)});it('never admits concepts or unapproved captures as goldens',()=>{expect(()=>requireApprovedGolden({source:'CONCEPT',approvedBy:'a',approvedAt:'2026-01-01'})).toThrow();expect(()=>requireApprovedGolden({source:'DEVICE_CAPTURE',approvedBy:null,approvedAt:null})).toThrow();expect(()=>requireApprovedGolden({source:'DEVICE_CAPTURE',approvedBy:'human',approvedAt:'2026-01-01'})).not.toThrow()})});
