# open-manual

게임 앱을 하나 출시하면서 실제로 부딪힌 것과 그 해결을 적는다. 다음 앱에서 같은 벽을 다시
만나지 않기 위한 문서이므로, **이 저장소에만 해당하는 설정은 적지 않는다.** 그런 것은
[`CLAUDE.md`](CLAUDE.md)에 있다.

## 이 문서를 쓰는 방법

- **사건이 끝난 직후에 적는다.** 끝나고 몰아 쓰면 이미 잊은 뒤다. 증상만 남고 왜 그렇게
  판단했는지가 사라지는데, 다음에 필요한 건 그 판단 과정 쪽이다.
- **틀린 판단도 적는다.** 오진에 쓴 시간이 정답보다 비싸다. §9의 항목 절반이 오진 기록이다.
- **한 항목은 "증상 → 진짜 원인 → 왜 헷갈렸나 → 다음엔 무엇을 먼저 보나" 네 줄이면 된다.**

---

## 1. 출시 일정에는 줄일 수 없는 구간이 있다

안드로이드 개인 개발자 계정 기준, 계정 생성부터 프로덕션까지 **실질 하한이 약 3주**다.

```
D0–D5    신원 확인 심사 (2~5 영업일)
         이 동안 스토어 등록정보 작성과 빌드 업로드는 되지만 어떤 트랙에도 게시 불가
D5       앱 생성 → 첫 AAB 업로드 → App Signing 인증서 지문 발급
D5–D19   비공개 테스트: 테스터 12명이 14일 연속 opt-in   ← 단축 불가
D19–D26  프로덕션 액세스 신청 → 심사 (보통 7일 이내)
```

**14일 구간에는 사람을 더 붙여도 소용없다.** 그리고 이 구간은 세 가지로 리셋된다.

- 테스터가 12명 아래로 내려가면 카운트가 처음부터 다시
- 내부 테스트(Internal) 트랙은 **하루도 산입되지 않는다.** 반드시 비공개(Closed) 트랙
- 조직 계정에는 이 요건이 없다. 개인 계정만의 규정이다

실무적으로 가장 큰 단축은 **계정 만드는 날 테스터를 모으기 시작하는 것**이다. 12명을 실제로
모으는 데 걸리는 시간이 일정에서 가장 과소평가된다.

### 코드보다 먼저 시작해야 하는 것

계정 등록은 코드 완성도와 무관하다. 기능이 절반이어도 등록·신원확인·테스터 모집은 진행된다.
**달력이 걸린 항목을 코드 작업과 직렬로 놓지 않는다.**

---

## 2. 초록불이 거짓말하는 세 가지 방식

이 프로젝트에서 가장 비싼 사건은 전부 **게이트가 통과시킨 결함**이었다. 유형이 셋이다.

### 2-1. 검사가 결함이 있는 자리를 안 본다

두 번 겪었다.

| 사건 | 검사가 본 곳 | 결함이 있던 곳 |
| --- | --- | --- |
| 로그인 폴리필 회귀 | 테스트·번들 전역 | 어느 테스트도 밟지 않는 전역 객체 |
| 구도 격자 검출 | 3분할선 4개 | 5×5 격자 (열 205/410/614/819) |

격자 쪽 숫자가 특히 선명하다. 검사기가 표본으로 삼은 x=341은 덮임 0.14, x=683은 0.03이었다.
**격자가 화면에 뚜렷이 보이는데 검사기는 "0 affected"라고 답했다.**

> 검사를 새로 쓸 때는 "이 검사가 통과하면서도 존재할 수 있는 결함"을 한 번 적어본다.
> 그게 비어 있지 않으면 검사가 아니라 의식(儀式)이다.

### 2-2. 픽스처가 그 기능을 꺼 놓았다

- 마감 후 힌트가 사라진 버그: 픽스처에 `hintUnits`가 없어 힌트 버튼이 렌더될 수조차 없었다.
  어떤 단언을 써도 통과했을 것이다.
- 로컬 게임 픽스처: 시즌을 `{"localFixture": true}` 같은 자리표시자 콘텐츠에 pin했다.
  랭킹 표는 채워지지만 **판은 절대 그려지지 않는다.** 그래서 이 저장소는 게임을 한 번도
  돌려본 적이 없으면서 관련 테스트는 전부 초록이었다.

> 단언을 쓰기 전에 **픽스처가 그 경로를 켜는지** 본다.

### 2-3. 종료 코드가 다른 것의 종료 코드다

배경 실행이나 파이프를 거치면 게이트의 결과가 아니라 **래퍼의 결과**가 보고된다.

```bash
# 나쁨 — tail의 종료 코드가 보인다. 실패한 게이트가 0으로 읽힌다
pnpm check | tail -20

# 좋음 — 파일 안의 EXIT= 줄만 믿는다
pnpm check > gate.log 2>&1; echo "EXIT=$?" >> gate.log
grep "EXIT=" gate.log
```

이번 세션에서 **알림이 "exit code 0"이라고 말한 게이트가 실제로는 세 번 중 두 번 빨간불**
이었다. 매번 `EXIT=` 줄을 직접 읽어서 잡았다.

---

## 3. 생성 산출물은 사슬로 엮여 있다

파생 파일이 서로를 입력으로 쓰는 저장소에서는, 한 곳을 고치고 게이트를 돌리면 **다음 고리가
하나씩 빨간불로 나온다.** 이번에 10분짜리 게이트를 세 번 연속 낭비했다.

```
룰셋/콘텐츠 변경
  → 파생 히트박스        (아트 판정)
  → 워드헌트 검사        (사용 불가 팩의 좌표는 실패)
  → 프리뷰 레지스트리    (엔트리 수를 테스트가 고정)
  → 인벤토리             (커밋본과 재빌드 비교)
  → 스펙 문서 → 요구사항 레지스트리 → 승인 해시
```

> **전부 재생성한 다음에 게이트를 한 번 돌린다.** 한 칸 고치고 게이트에 물어보지 않는다.

관련해서, 해시로 고정된 승인 아티팩트가 있으면 규칙이 하나 더 붙는다. 승인 항목이 스펙 문서의
**문장에서 숫자를 추출해** 대조하는 구조라면, 설계를 바꿀 때 문장까지 같이 고쳐야 하고 그
문장이 만드는 토큰이 승인 값과 일치해야 한다.

---

## 4. 계약이 콘텐츠보다 먼저 굳으면 콘텐츠가 못 들어온다

게임 규칙을 스키마와 DB 술어에 고정해 두었는데, **그 규칙을 만족하는 아트가 하나도 없었다.**

- 규칙: 한 판에 차이점 정확히 10개 (일반 7 + 어려움 3)
- 현실: 아트 136개 중 10개 이상이 나오는 건 41개. 승인된 5개는 5~9개
- 임계값을 낮춰 더 찾으려 하면 **실제 차이점이 아니라 텍스처 노이즈**가 잡힌다

결과적으로 규칙을 범위(5~20)와 비율로 바꿨고, 게시 가능 팩이 5개에서 77개가 됐다.

> 콘텐츠 개수를 계약에 **고정값**으로 박기 전에, 실제 자산이 그 값을 만족하는지 먼저 센다.
> 범위와 비율로 쓰면 나중에 자산이 바뀌어도 계약을 다시 승인받지 않아도 된다.

### 곁가지: 두 언어가 같은 정수를 내야 한다

비율을 쓸 때 부동소수점을 그대로 쓰면 안 된다.

```
round(n * 0.3)   n=5 → JS 1, Postgres numeric 2      ← 갈린다
(2*3*n + 10)/20  n=5 → 양쪽 2                        ← 정수 연산
```

`0.3`은 IEEE754에서 0.2999…다. 도구에서 통과한 콘텐츠를 DB가 거부하는 형태로 터진다.

---

## 5. 콘텐츠 좌표는 기계가 검사할 수 없는 부분이 남는다

검사기는 좌표의 **규칙**(반경 하한, 프레임 이탈, 중복, 겹침)을 본다. **그 좌표가 실제로 그
물건 위에 있는지는 못 본다.** 그리고 그게 틀리면 최악이다 — 프롬프트가 물건을 지목하고,
플레이어가 그 물건을 누르고, 게임이 틀렸다고 답한다.

측정된 실태:

| 출처 | 실제 대상 위에 있던 비율 |
| --- | --- |
| 손으로 적은 차이점 좌표 770개 | **49%** |
| 손으로 적은 서든데스 좌표 77개 | **25 / 77** |
| 드래프트의 워드헌트 히트박스 315개 | 286개가 차이점 좌표를 그대로 복사한 것 |

**계산할 수 있는 것은 계산한다.** "두 그림이 어디서 다른가"는 산수다. 이미지를 비교해
클러스터링하면 나온다. "프리즘이 어디 있는가"는 산수가 아니라서 사람이 놓아야 한다.

> 자동화 가능한 좌표를 손으로 적게 두면, 검사기가 통과시키는 잘못된 좌표가 쌓인다.

### 내용이 틀린 것과 모양이 맞는 것은 다르다

드래프트가 스키마를 전부 만족하면서 내용이 복사-붙여넣기인 경우가 있었다. 빵집 그림의 정답이
`coral reef`, 여섯 팩의 정답이 전부 `resilience`. **필드가 다 채워져 있다는 것은 내용이
맞다는 뜻이 아니다.** 자동 검사로는 절대 안 잡힌다.

---

## 6. 인프라 선택이 법무 문서를 바꾼다

기술 결정으로 보이는 것이 문서 작업량을 바꾼다.

| 결정 | 기술적 영향 | 문서 영향 |
| --- | --- | --- |
| DB 리전을 국내로 | 지연시간 소폭 | **국외 이전 고지 조항이 통째로 없어진다** |
| 무료 플랜 | 자동 백업 없음 | 보존 기간을 약속할 근거가 없다 |
| 유료 기본 플랜 | 일일 백업 N일 | 보존 기간이 플랜에서 확정된다 |

> 개인정보처리방침의 값은 "정하는" 게 아니라 **인프라에서 파생되는** 것이 많다. 리전과 백업
> 설정을 먼저 정하면 문서가 따라온다. 반대로 하면 문서를 두 번 쓴다.

### 법무 문서에 추정값을 넣지 않는다

미결 값은 눈에 띄게 미결로 렌더하고, 배포만 막고 저장소는 초록으로 둔다. 빈칸은 "생각하지
않았다"로 읽히고, 표시된 미결은 "아직 안 끝났다"로 읽힌다. 후자가 사실이다.

지어낸 값은 그 자체로 허위 기재다. 이전 판에 **존재하지 않는 지원 이메일 주소**와 **앱에 없는
크래시 로그 수집 신고**가 들어가 있었다.

---

## 7. 마이그레이션은 스스로 완결되어야 이식된다

호스티드 DB로 승격할 때 가장 흔한 실패는 **로컬에만 있던 롤·확장·설정**이다.

이 저장소는 마이그레이션이 커스텀 롤 9개를 `create role ... if not exists`로 직접 만든다.
그래서 `db push` 한 번으로 호스티드 프로젝트에 그대로 올라간다.

> 마이그레이션이 참조하는 모든 롤·확장을 마이그레이션 안에서 만든다. 로컬 셸에서 한 번
> `create role` 해놓고 잊으면, 승격 시점에 원인을 찾기 어려운 권한 오류로 나온다.

### 배포 경로가 테스트 전용으로 잠겨 있지 않은지 본다

"나중에 승인 워크플로가 생기면 열겠다"며 테스트 픽스처 전용으로 만들어 둔 함수가, 알고 보니
**프로덕션에서 반드시 필요한 행을 쓸 수 있는 유일한 경로**였다. 마이그레이션만으로 만든 DB에서
외래키 위반으로 즉시 실패해서야 발견했다.

> 기능을 "테스트 전용"으로 잠글 때, 그 기능이 프로덕션 경로의 **유일한** 출구인지 확인한다.

---

## 8. 로컬 환경 — 반복해서 무너지는 지점

프로젝트마다 다르지만, 유형은 반복된다.

- **파일 핸들을 잡는 개발 서버.** 번들러가 프로젝트 전체를 크롤하면서 빌드 디렉터리를 붙잡아
  네이티브 빌드가 실패한다. 원인이 빌드 도구처럼 보인다. **번들러를 먼저 내린다.**
- **컨테이너 스택이 헬스체크에서 롤백.** 스키마가 크면 모든 컨테이너가 동시에 질의하다
  타임아웃 난다. 메모리를 늘리면 오히려 나빠진다. **안 쓰는 서비스를 빼는 게 답이다.**
- **고아 소켓.** 하드 종료 뒤 컨테이너 런타임이 소켓 파일에 걸려 시작하지 못한다. 삭제는
  안 되고 이름 변경만 된다. **디렉터리가 하나가 아니라 여러 개일 수 있다** — 하나만 고치면
  에러 메시지만 바뀌어서 새 문제처럼 보인다.
- **핫 리로드는 모듈 스코프를 다시 평가하지 않는다.** 새로 추가한 모듈 상수가
  `ReferenceError`로 죽는다. "고쳤는데 왜 그대로지"의 대부분이 이것이다. 전체 리로드가 필요하다.
- **에뮬레이터가 죽었는데 프로세스는 살아 있다.** adb에 안 잡히고 로그도 중간에 멈춘다.
  실제로는 크래시 리포터 대화상자다. **화면을 직접 봐야 안다.** 창을 없애면(헤드리스)
  그 그래픽 경로를 통째로 우회한다.

---

## 9. 사건 기록

새 사건은 **맨 위에** 추가한다. 형식은 증상 / 원인 / 오진 / 다음 대응.

### 2026-08-27 · 소스에 원시 NUL 바이트가 들어가 git이 바이너리로 취급

- **증상** — `git show --stat`이 TypeScript 파일을 `Bin 28676 -> 29071 bytes`로 표시. diff가 안 나온다.
- **원인** — 해시 구분자로 쓰려던 자리에 이스케이프(`\0`)가 아니라 **원시 NUL 바이트 1개**가 들어갔다.
- **오진** — 파일이 UTF-8로는 정상 디코딩되고 타입체크·테스트·실행이 전부 통과해서 한참 못 봤다.
- **다음** — 커밋 후 `git show --stat`에 `Bin`이 뜨면 즉시 확인한다. 텍스트 파일이 바이너리로
  잡히는 건 항상 이상 신호다.

### 2026-08-27 · 손으로 관리하는 제외 목록이 조용히 비어 있었다

- **증상** — 불량 아트를 걸러야 할 `DEFECTIVE_ART` Set이 빈 채로 있었다.
- **원인** — 바로 위 주석 **두 개**가 특정 팩을 계속 지목하고 있었는데 Set만 비워졌다. 아무도 못 잡았다.
- **다음** — 제외 목록은 손으로 쓰지 않고 **판정 단계가 증거로 거부**하게 만든다. 그러면 목록과
  현실이 어긋날 자리가 없다. 이 저장소는 히트박스 판정에서 이미 그 패턴을 쓰고 있었고,
  아트 결함만 손 목록으로 남아 있었다.

### 2026-08-27 · 명시적 금지를 못 보고 반대로 구현

- **증상** — 불량 아트를 격리 목록으로 처리했는데 게이트가 빨간불.
- **원인** — 그 테스트 주석에 *"replace that artwork — do not extend this list"*라고
  적혀 있었다. 저장소가 글로 금지한 방식을 구현한 것.
- **다음** — 게이트가 막으면 **먼저 그 검사의 주석을 읽는다.** 왜 그렇게 만들었는지가 대개
  적혀 있고, 우회가 아니라 의도를 읽어야 할 신호다.

### 2026-08-27 · 정책은 열려 있는데 콘텐츠를 넣을 수단이 없었다

- **증상** — "게임플레이가 정책 승인에서 막혀 있다"고 인수인계됨.
- **원인** — 실제로 정책은 이미 열려 있었다. 진짜 원인은 **승인된 콘텐츠를 DB에 게시하는 도구가
  존재하지 않았던 것.** 로컬 픽스처가 자리표시자 콘텐츠로 시즌을 채워서 이 사실을 가리고 있었다.
- **다음** — 인수인계 문서의 차단 원인을 그대로 믿지 않는다. **직접 재본다.**

### 2026-08-27 · 게이트 알림의 종료 코드가 게이트의 것이 아니었다

- **증상** — 완료 알림이 exit 0인데 실제 게이트는 실패.
- **원인** — 배경 실행 래퍼의 종료 코드. §2-3 참조.
- **다음** — 로그 파일의 `EXIT=` 줄만 믿는다. 세 번 중 두 번이 실제로는 빨간불이었다.

---

## 10. 다음 앱을 시작할 때 순서

1. **달력이 걸린 것부터 시작한다** — 스토어 계정 등록, 신원 확인, 테스터 모집. 코드와 병렬로.
2. **인프라 결정을 법무 문서보다 먼저 한다** — 리전, 백업 플랜. 문서 값이 여기서 파생된다.
3. **콘텐츠 계약을 굳히기 전에 실제 자산을 센다** — 고정값 대신 범위와 비율로.
4. **자동 계산 가능한 좌표·값은 처음부터 계산한다** — 손으로 적으면 검사기가 못 잡는다.
5. **마이그레이션이 스스로 완결되게 쓴다** — 롤·확장을 안에서 만든다.
6. **새 검사를 쓸 때 "이 검사가 통과하면서도 존재할 수 있는 결함"을 적어본다.**
