import fs from'node:fs';import path from'node:path';import{runEconomySimulation,runMatchSimulation}from'./release-evidence.js';
const reportRootArg=process.argv.find(x=>x.startsWith('--report-root=')),root=reportRootArg?path.resolve(reportRootArg.slice('--report-root='.length)):process.cwd(),reports=[['docs/testing/reports/match-bot-v1-seed-20260719.json',runMatchSimulation({seed:20260719,matches:10000,botModelVersion:'bot-v1'})],['docs/testing/reports/economy-draw-v1-seed-20260719.json',runEconomySimulation({seed:20260719,draws:100000})]]as const,check=process.argv.includes('--check');let drift=0;for(const[file,value]of reports){const expected=`${JSON.stringify(value,null,2)}\n`,target=path.join(root,file);if(check){if(!fs.existsSync(target)||fs.readFileSync(target,'utf8')!==expected){console.error(`stale report: ${file}`);drift++;}}else{fs.mkdirSync(path.dirname(target),{recursive:true});fs.writeFileSync(target,expected);}}if(drift)process.exit(1);console.log(`release reports ${check?'fresh':'written'}: 10000 matches, 100000 draws`);
