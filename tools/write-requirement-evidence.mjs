import fs from'node:fs';const registry=JSON.parse(fs.readFileSync('docs/requirements-registry.v1.json','utf8')),entries=registry.requirements.map(({id,evidenceKind,oracle,metric,linkageProof})=>({id,evidenceKind,oracle,metric,linkageProof}));fs.writeFileSync('config/requirement-evidence.v1.json',JSON.stringify({schemaVersion:1,entries},null,2)+'\n');const ids=entries.map(x=>x.id);fs.writeFileSync('tests/specs/generated-requirement-coverage.test.ts',`import fs from 'node:fs';import {describe,expect,it}from'vitest';\nconst claimedIds=${JSON.stringify(ids)} as const;\ndescribe('generated exact requirement evidence',()=>{it.each(claimedIds)('explicitly asserts %s',(id)=>{const registry=JSON.parse(fs.readFileSync('docs/requirements-registry.v1.json','utf8'));const evidence=JSON.parse(fs.readFileSync('config/requirement-evidence.v1.json','utf8'));const row=registry.requirements.find((x:{id:string})=>x.id===id),claim=evidence.entries.find((x:{id:string})=>x.id===id);expect(row).toBeTruthy();expect(claim).toMatchObject({id,evidenceKind:'EXECUTABLE_PROJECTION',metric:null});expect(claim.oracle).toContain(id);expect(fs.existsSync(row.schema)).toBe(true);expect(row.fingerprint).toMatch(/^[a-f0-9]{64}$/);});});\n`);
