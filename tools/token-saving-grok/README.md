# token-saving-grok portable bundle

See `../../token-saving-grok-portable-install.md` or `../../docs/token-saving-grok-portable-install.md`.

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\token-saving-grok\install.ps1 `
  -ProjectRoot (Get-Location).Path -RtkMode enforce
```
