import{createHash}from'node:crypto';import{readFile,stat,writeFile}from'node:fs/promises';import sharp from'sharp';import{canonicalJsonSha256}from'../../packages/contracts/src/canonical-json.js';import type{DeltaRegion}from'./visual-delta.js';
type Meaning={prompt:string;options:Array<{id:string;label:string}>;correctOptionId:string};export type LearningBundleInput={key:string;language:'en'|'ko';canonicalAnswer:string;aliases:string[];meaning:Meaning};export type LearningGeometry={differences?:Array<DeltaRegion&{tier:'NORMAL'|'HARD'}>;wordHunts?:Array<DeltaRegion&{kind:'NORMAL'|'SPECIAL';publicPrompt:string}>;suddenDeath?:DeltaRegion};
const sha=async(file:string)=>createHash('sha256').update(await readFile(file)).digest('hex'),uuid=(value:string)=>{const h=createHash('sha256').update(value).digest('hex');return`${h.slice(0,8)}-${h.slice(8,12)}-4${h.slice(13,16)}-8${h.slice(17,20)}-${h.slice(20,32)}`;};
async function descriptor(file:string){const[hash,info,meta]=await Promise.all([sha(file),stat(file),sharp(file).metadata()]);if(!meta.width||!meta.height||meta.format!=='png')throw Error('LEARNING_ASSET_MUST_BE_PNG');return{url:`https://cdn.spot-learn.test/assets/${hash}.png`,sha256:hash,encodedBytes:info.size,width:meta.width,height:meta.height,mimeType:'image/png' as const};}
export async function writeLearningBundle(entry:LearningBundleInput,imageA:string,imageB:string,output:string,geometry:LearningGeometry={}){const[a,b]=await Promise.all([descriptor(imageA),descriptor(imageB)]);if(a.width!==b.width||a.height!==b.height)throw Error('PAIR_DIMENSION_MISMATCH');const revision=uuid(`${entry.key}:${a.sha256}:${b.sha256}`),body={contentRevisionId:revision,schemaVersion:'1.0.0',differences:(geometry.differences??[]).map(x=>({objectiveId:x.id,tier:x.tier,hitboxes:{imageA:{cx:x.cx,cy:x.cy,r:x.r},imageB:{cx:x.cx,cy:x.cy,r:x.r}}})),wordHunts:(geometry.wordHunts??[]).map(x=>({missionId:x.id,kind:x.kind,publicPrompt:x.publicPrompt,hitboxes:{imageA:{cx:x.cx,cy:x.cy,r:x.r},imageB:{cx:x.cx,cy:x.cy,r:x.r}}})),suddenDeath:geometry.suddenDeath?{objectiveId:geometry.suddenDeath.id,hitboxes:{imageA:{cx:geometry.suddenDeath.cx,cy:geometry.suddenDeath.cy,r:geometry.suddenDeath.r},imageB:{cx:geometry.suddenDeath.cx,cy:geometry.suddenDeath.cy,r:geometry.suddenDeath.r}}}:null,finalChallenge:{canonicalAnswer:entry.canonicalAnswer,aliases:entry.aliases,hintUnits:[...new Intl.Segmenter(entry.language,{granularity:'grapheme'}).segment(entry.canonicalAnswer)].map(x=>x.segment),meaning:entry.meaning}},result={schemaVersion:'1.0.0',status:'DRAFT',rightsReviewStatus:'REVIEW_REQUIRED',educationReviewStatus:'REVIEW_REQUIRED',publicContent:{contentId:uuid(entry.key),version:1,contentRevisionId:revision,schemaVersion:'1.0.0',assetPolicyVersion:'1.0.0',theme:entry.key,language:entry.language,difficulty:'ADVANCED',imageA:a,imageB:b},privateSolution:{...body,privateSolutionHash:canonicalJsonSha256(body)},assetFiles:{[a.sha256]:imageA,[b.sha256]:imageB}};await writeFile(output,`${JSON.stringify(result,null,2)}\n`,'utf8');return result;}
