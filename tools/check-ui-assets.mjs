import {Ajv2020} from 'ajv/dist/2020.js';import addFormats from 'ajv-formats';import {createHash} from 'node:crypto';import {readFileSync,existsSync} from 'node:fs';import {resolve,basename} from 'node:path';
const target=process.argv.includes('--target')?process.argv[process.argv.indexOf('--target')+1]:'contract';if(!['contract','beta'].includes(target)){console.error('target must be contract|beta');process.exit(2)}
const manifestPath=process.env.UI_ASSET_MANIFEST??'config/ui-runtime-assets.v1.json';const schema=JSON.parse(readFileSync('schemas/ui-runtime-assets.schema.json'));const value=JSON.parse(readFileSync(manifestPath));const ajv=new Ajv2020({strict:true,allErrors:true});addFormats(ajv);const validate=ajv.compile(schema);if(!validate(value)){console.error(validate.errors);process.exit(1)}
if(target==='beta'&&value.lifecycle!=='APPROVED'){console.error('beta requires APPROVED assets');process.exit(1)}
if(value.lifecycle==='APPROVED'){const seen=new Set();for(const entry of value.entries){if(seen.has(entry.assetId)||seen.has(entry.side))throw new Error('asset ids and sides must be one-to-one');seen.add(entry.assetId);seen.add(entry.side);const file=resolve(process.env.UI_ASSET_ROOT??'content/runtime-assets',basename(new URL(entry.url).pathname));if(!existsSync(file))throw new Error(`missing build reference ${file}`);const bytes=readFileSync(file);if(createHash('sha256').update(bytes).digest('hex')!==entry.sha256||bytes.length!==entry.encodedBytes)throw new Error('asset bytes drift');if(bytes.toString('ascii',1,4)!=='PNG')throw new Error('asset is not PNG');const width=bytes.readUInt32BE(16),height=bytes.readUInt32BE(20);if(width!==entry.width||height!==entry.height)throw new Error('asset dimensions drift')}}
console.log(`UiRuntimeAssetManifestV1 ${value.lifecycle}: ${value.entries.length} runtime assets; target=${target}`);
