import fs from'node:fs';import path from'node:path';
import{parse as parseYaml}from'yaml';
import{parseRuleset}from'../packages/contracts/src/rules.schema.js';
import{parseEconomy}from'../packages/contracts/src/economy.schema.js';
import{AnalyticsCollector,emitRequirementGateStatus,parseAnalyticsEventV1,validateLifecycleTrace,type RequirementGateResult}from'../packages/contracts/src/analytics.js';
import{evaluateLoadSamples}from'./release-evidence.js';
import{validateUiReferenceBundle}from'../packages/contracts/src/ui.js';
import{createTestingReplayBundle,testingRules}from'../packages/game-engine/src/testing-fixtures.js';import{reduceMatch}from'../packages/game-engine/src/reducer.js';import{replayMatch}from'../packages/game-engine/src/replay.js';
import{clientCommandEnvelopeSchema,negotiateCompatibility,resolveAuthenticatedParticipant}from'../packages/contracts/src/socket.schema.js';import{decideDelivery,decideReconnect}from'../packages/contracts/src/delivery-policy.js';import{projectSnapshot}from'../packages/contracts/src/projection.js';import{CONTENT_TEXT_LIMITS_V1,isWithinContentTextLimits,RECOMMENDED_IMAGE_LONG_EDGE_PX}from'../packages/contracts/src/content.js';import{reconnectOverlayForElapsed}from'../packages/game-engine/src/input-projection.js';import{adaptMatchSnapshot,createTapIntent}from'../apps/mobile/src/ui/battle-shell.js';import type{MatchStateV1}from'../packages/contracts/src/match.js';
import{MATCH_DB_PROJECTION_V1}from'../packages/contracts/src/match.js';import{deriveTerminalConstraintProjection}from'../packages/contracts/src/integration-evidence.js';
import{matchSnapshotV1Schema}from'../packages/contracts/src/socket.schema.js';
import{serverEventEnvelopeSchema,commandAckSchema}from'../packages/contracts/src/socket.schema.js';import{projectMatchEvent}from'../packages/contracts/src/projection.js';
import{publicGameContentSchema,privateGameSolutionSchema,rightsManifestSetSchema}from'../packages/contracts/src/content.js';
import{CONTENT_REQUIREMENT_CASES}from'../packages/content-validator/src/validate-content.js';
import{derivePlayerInputState,terminalPhaseForEndReason}from'../packages/game-engine/src/input-projection.js';import{contentToScreen,inverseContentPoint}from'../apps/mobile/src/ui/battle-shell.js';import{parseMatchCommandV1,parseMatchStateV1}from'../packages/contracts/src/match.schema.js';
const externalKinds=new Set(['EXACT_RUNTIME','DEVICE_GOLDEN','PROVIDER_CREDENTIALS','TARGET_REGION_SOAK','LEGAL_APPROVAL','RIGHTS_APPROVAL','PRODUCT_DECISION']);
export function validateNonCurrentEvidence(root:string,claim:{lifecycle:string;closureCondition?:string;closureArtifact?:string;closureArtifactState?:'ABSENT'|'PRESENT';externalKind?:string;localSubstitute?:string}){if(!claim.closureCondition)return false;if(claim.lifecycle==='PLANNED')return!!claim.closureArtifact&&claim.closureArtifactState==='ABSENT'&&!fs.existsSync(path.join(root,claim.closureArtifact));if(claim.lifecycle==='EXTERNAL')return externalKinds.has(claim.externalKind??'')&&!!claim.localSubstitute&&!fs.existsSync(path.join(root,claim.localSubstitute));return false;}

type Row={id:string;source:string;sourceLine:number;text:string};
type Assertion={pointer:string;expected:unknown};type Claim={lifecycle:'CURRENT'|'PLANNED'|'EXTERNAL';oracle:{kind:string;expected:RequirementGateResult;input?:string;assertions?:Assertion[]};blockerReason?:string;closureCondition?:string;closureArtifact?:string;closureArtifactState?:'ABSENT'|'PRESENT';externalKind?:string;localSubstitute?:string};
const resolve=(value:unknown,pointer:string)=>pointer.split('/').slice(1).reduce<unknown>((current,key)=>current!==null&&typeof current==='object'?(current as Record<string,unknown>)[key]:undefined,value);
const assertExact=(value:unknown,assertions:Assertion[]|undefined)=>{if(!assertions?.length)throw Error('exact assertions required');for(const assertion of assertions)if(JSON.stringify(resolve(value,assertion.pointer))!==JSON.stringify(assertion.expected))throw Error(`SSOT mismatch ${assertion.pointer}`);};
type OpenApiOperation={parameters?:Array<{$ref:string}>;requestBody?:{$ref:string};responses:Record<string,{$ref:string}>};
type OpenApiDocument={security?:unknown;paths:Record<string,Record<string,OpenApiOperation>>;components:{schemas:Record<string,{properties?:{code?:{enum?:string[]}}}>}};
const REST_REQUIREMENTS:Record<string,{method:'get'|'post';path:string;request:null|string;success:string;statuses:string[];errors:string[]}>= {
 'API-001':{method:'get',path:'/v1/me',request:null,success:'MeResponse',statuses:['200','401'],errors:['UNAUTHORIZED']},
 'API-002':{method:'get',path:'/v1/pets',request:null,success:'PetsResponse',statuses:['200','401'],errors:['UNAUTHORIZED']},
 'API-003':{method:'post',path:'/v1/pets/{id}/lock',request:'LockPetRequest',success:'LockPetResponse',statuses:['200','404','409'],errors:['NOT_OWNED','IDEMPOTENCY_CONFLICT']},
 'API-004':{method:'post',path:'/v1/pets/{id}/select',request:null,success:'SelectPetResponse',statuses:['200','404','409'],errors:['NOT_OWNED','IDEMPOTENCY_CONFLICT']},
 'API-006':{method:'post',path:'/v1/gacha/draw',request:null,success:'GachaDrawResponse',statuses:['200','409'],errors:['IDEMPOTENCY_CONFLICT','POLICY_MISMATCH','INSUFFICIENT_FUNDS']},
 'API-007':{method:'post',path:'/v1/fusion',request:'FusionRequest',success:'FusionResponse',statuses:['200','400','404','409'],errors:['INVALID_MATERIALS','NOT_OWNED','IDEMPOTENCY_CONFLICT','POLICY_MISMATCH']},
 'API-008':{method:'post',path:'/v1/matches/queue',request:'QueueCreateRequest',success:'QueueTicketResponse',statuses:['202','400','409'],errors:['INVALID_REQUEST','CONFLICT','IDEMPOTENCY_CONFLICT']},
 'API-009':{method:'post',path:'/v1/matches/friend-room',request:'FriendRoomCreateRequest',success:'FriendRoomResponse',statuses:['201','400','409'],errors:['INVALID_REQUEST','CONFLICT','IDEMPOTENCY_CONFLICT']},
};
const refName=(ref:string)=>ref.split('/').at(-1);
export function evaluateOpenApiRequirement(id:string,provided?:string){const expected=REST_REQUIREMENTS[id];if(!expected)throw Error('unsupported REST requirement');const source=provided??fs.readFileSync(path.resolve('packages/contracts/openapi.yaml'),'utf8'),document=parseYaml(source)as OpenApiDocument;if(JSON.stringify(document.security)!==JSON.stringify([{bearerAuth:[]}]))throw Error('bearer security drift');const operation=document.paths?.[expected.path]?.[expected.method];if(!operation)throw Error('REST operation missing');const parameterRefs=(operation.parameters??[]).map(x=>x.$ref),idempotency='#/components/parameters/IdempotencyKey';if(expected.method==='get'&&parameterRefs.includes(idempotency))throw Error('GET idempotency drift');if(expected.method!=='get'&&!parameterRefs.includes(idempotency))throw Error('mutation idempotency drift');const actualRequest=operation.requestBody?refName(operation.requestBody.$ref):null;if(actualRequest!==expected.request)throw Error('request schema drift');const statuses=Object.keys(operation.responses??{}).sort();if(JSON.stringify(statuses)!==JSON.stringify([...expected.statuses].sort()))throw Error('status set drift');const successStatus=expected.statuses[0];if(!successStatus||refName(operation.responses[successStatus]?.$ref??'')!==expected.success)throw Error('success response schema drift');const errors=expected.statuses.slice(1).flatMap(status=>{const schema=refName(operation.responses[status]?.$ref??'');return document.components.schemas[schema??'']?.properties?.code?.enum??[];});if(JSON.stringify([...new Set(errors)].sort())!==JSON.stringify([...expected.errors].sort()))throw Error('error code set drift');return true;}
export type DatabaseRequirementSource={sql:string;config:string;roles:string};
const requireSql=(source:DatabaseRequirementSource,needles:string[])=>{for(const needle of needles)if(!source.sql.toLowerCase().includes(needle.toLowerCase()))throw Error(`database predicate missing: ${needle}`);};
export function evaluateDatabaseRequirement(id:string,provided?:DatabaseRequirementSource){const root=process.cwd(),read=(p:string)=>fs.readFileSync(path.join(root,p),'utf8'),source=provided??{sql:fs.readdirSync(path.join(root,'supabase/migrations')).filter(x=>x.endsWith('.sql')).sort().map(x=>read(`supabase/migrations/${x}`)).join('\n'),config:read('supabase/config.toml'),roles:read('supabase/roles.sql')};
 const sql=source.sql.replace(/--[^\n]*/gu,'').replace(/\s+/gu,' ').trim().toLowerCase(),has=(pattern:RegExp,message:string)=>{if(!pattern.test(sql))throw Error(message);};
 if(id==='DATA-001'){if(!/^schemas\s*=\s*\["public",\s*"graphql_public"\]/mu.test(source.config)||source.config.match(/^schemas\s*=/gmu)?.length!==1)throw Error('Data API schema exposure drift');requireSql(source,['create schema if not exists private','revoke all on schema private from public, anon, authenticated, service_role','alter default privileges for role postgres in schema private revoke all on tables from public, anon, authenticated, service_role']);return true;}
 if(id==='DATA-002'){requireSql(source,['create table public.game_content_revisions','create table private.game_content_solutions','create table private.content_rights_manifests','create table private.content_publish_attestations','create view public.game_content_catalog']);return true;}
 if(id==='DATA-003'){requireSql(source,['unique(content_id, version)','public_content_hash text not null unique','schema_version text not null','asset_policy_version text not null','validator_version text not null','status text not null','approved_at timestamptz','game_content_revisions_immutable']);return true;}
 if(id==='DATA-004'){requireSql(source,['content_revision_id uuid primary key references public.game_content_revisions(content_revision_id) on delete restrict','private_solution jsonb not null','private_solution_hash text not null unique','game_content_solutions_immutable']);return true;}
 if(id==='DATA-005'){requireSql(source,['create table private.content_rights_manifests','manifest jsonb not null','manifest_hash text not null unique','content_rights_manifests_immutable']);return true;}
 if(id==='DATA-006'){requireSql(source,['create table private.content_publish_attestations','validator_version text not null','public_content_hash text not null','database_role text not null','session_role text not null','invoked_role text not null','content_publish_attestations_immutable']);return true;}
 if(id==='DATA-007'){requireSql(source,['create view public.game_content_catalog','with (security_invoker = true)',"where status = 'PUBLISHED' and approved_at is not null",'grant select on public.game_content_catalog to anon, authenticated','revoke all on public.game_content_catalog from public, service_role']);return true;}
 if(id==='DATA-008'){requireSql(source,['private.legacy_game_contents_quarantine','private.legacy_match_events_quarantine','private.legacy_matches_quarantine','to_jsonb(game_contents)','to_jsonb(match_events)','drop table public.match_events','drop table public.game_contents']);return true;}
 if(id==='DATA-009'){requireSql(source,['create role game_security_owner nologin noinherit nosuperuser nocreatedb nocreaterole noreplication','owner to game_security_owner']);return true;}
 if(id==='DATA-010'){requireSql(source,['create role deployment_role nologin noinherit nosuperuser nocreatedb nocreaterole noreplication','grant execute on function private.publish_content_revision_v1(jsonb,jsonb,jsonb,text,text,text,text) to deployment_role','revoke execute on function private.publish_content_revision_v1(jsonb,jsonb,jsonb,text,text,text,text) from public, anon, authenticated, service_role, app_server','revoke execute on function private.publish_economy_bundle_v1(jsonb,jsonb) from deployment_role','from deployment_role;\ngrant execute on function private.claim_admin_publish_v1']);return true;}
 if(id==='DATA-011'){requireSql(source,['create role app_server nologin noinherit nosuperuser nocreatedb nocreaterole noreplication','grant execute on function private.join_match_participant_v1(uuid,uuid,uuid) to app_server','revoke execute on function private.join_match_participant_v1(uuid,uuid,uuid) from public, anon, authenticated, service_role, deployment_role','private.set_pet_lock_v1(uuid,uuid,text,uuid,boolean) from app_server']);return true;}
 if(id==='DATA-012'){requireSql(source,['grant game_security_owner to postgres','revoke game_security_owner from postgres']);if(!source.roles.includes('grant app_server, deployment_role, economy_deployment_role, economy_server, admin_publish_role to postgres')||!source.roles.includes('do not deploy this file with --include-roles'))throw Error('local role bootstrap boundary drift');return true;}
 if(id==='DATA-013'){for(const signature of ['private.publish_content_revision_v1(jsonb,jsonb,jsonb,text,text,text,text)','private.join_match_participant_v1(uuid,uuid,uuid)'])requireSql(source,[`alter function ${signature} owner to game_security_owner`,`revoke execute on function ${signature} from public, anon, authenticated, service_role`,`grant execute on function ${signature}`]);if((source.sql.match(/security definer/giu)?.length??0)<2||(source.sql.match(/set search_path = pg_catalog/giu)?.length??0)<2)throw Error('definer hardening drift');return true;}
 if(id==='DATA-014'){has(/create type public\.match_status as enum \( 'waiting_for_assets','countdown','playing','final_rush','settling', 'tiebreak_eval','sudden_death','finished','cancelled' \)/u,'match enum drift');return true;}
 if(id==='DATA-015'){for(const column of ['content_revision_id uuid not null references public.game_content_revisions','ruleset_version text not null','ruleset_hash text not null','engine_version text not null','protocol_version text not null','experiment_variant text not null'])if(!sql.includes(column))throw Error(`pinned column drift ${column}`);return true;}
 if(id==='DATA-016'){for(const p of [/alter table public\.match_players alter column user_id drop not null/u,/add column participant_key uuid not null/u,/foreign key \(user_id\) references public\.profiles\(id\) on delete set null/u,/primary key \(match_id, participant_key\)/u])has(p,'participant identity drift');return true;}
 if(id==='DATA-017'){for(const p of [/check \(seat_no in \(1,2\)\)/u,/unique \(match_id, seat_no\)/u,/unique index match_players_match_user_unique on public\.match_players\(match_id, user_id\) where user_id is not null/u,/status = 'waiting_for_assets' for update/u,/seat_no=1[\s\S]*seat_no=2[\s\S]*return false/u])has(p,'seat concurrency drift');return true;}
 if(id==='DATA-018'){for(const p of [/foreign key \(id, winner_participant_key\) references public\.match_players\(match_id, participant_key\) deferrable initially deferred/u,/constraint matches_terminal_shape check/u,/status = 'cancelled'[\s\S]*winner_participant_key is null/u,/status = 'finished'[\s\S]*end_reason = 'draw'/u])has(p,'terminal shape drift');return true;}
 if(id==='DATA-019'){for(const p of [/profiles_level_positive check \(level >= 1\)/u,/profiles_exp_nonnegative check \(exp >= 0\)/u,/profiles_points_nonnegative check \(gacha_points >= 0\)/u,/user_pets_level_positive check \(level >= 1\)/u,/user_pets_exp_nonnegative check \(exp >= 0\)/u,/user_pets_copies_positive check \(copies >= 1\)/u,/match_players_score_nonnegative check \(score >= 0\)/u,/unique index user_pets_one_selected_per_user on public\.user_pets\(user_id\) where selected/u])has(p,'bounded value drift');return true;}
 if(id==='DATA-020'){has(/create table private\.accepted_objective_claims[\s\S]*primary key\(match_id, objective_id\)/u,'objective uniqueness drift');return true;}
 if(id==='DATA-021'){for(const p of [/create table private\.match_request_receipts/u,/create table private\.match_command_receipts/u,/create table private\.match_events/u,/match_command_receipts_immutable before update or delete/u,/match_events_immutable before update or delete/u,/old\.status = 'completed'[\s\S]*new\.status <> 'completed'/u,/unique\(match_id, event_seq\)/u,/event_id = match_id::text \|\| ':' \|\| event_seq::text/u])has(p,'receipt event invariant drift');return true;}
 if(id==='DATA-022'){for(const p of [/create table private\.g3_command_receipts/u,/create table private\.g3_journal/u,/create table private\.g3_snapshots/u,/create table private\.g3_timer_intents/u,/create table private\.g3_effect_outbox/u,/function private\.apply_match_command_g3/u,/select \* into r from private\.g3_command_receipts[\s\S]*for update/u,/r\.request_hash<>p_hash[\s\S]*idempotency_conflict/u,/r\.fence<>p_expected_fence[\s\S]*stale_fence/u,/insert into private\.g3_journal[\s\S]*insert into private\.g3_snapshots[\s\S]*insert into private\.g3_timer_intents[\s\S]*insert into private\.g3_effect_outbox[\s\S]*update private\.g3_command_receipts set status='completed'/u,/p_fault='after_claim'[\s\S]*p_fault='after_journal'[\s\S]*p_fault='after_effect'/u,/security definer set search_path=pg_catalog/u,/grant execute on function private\.apply_match_command_g3\(uuid,uuid,text,bigint,jsonb,jsonb,text,bigint,text,text\) to app_server/u])has(p,'G3 transaction adapter drift');return true;}
 if(id==='DATA-023'){for(const p of [/policy pet_catalog_public_read[\s\S]*for select to anon, authenticated using \(active\)/u,/grant select on public\.pet_catalog to anon, authenticated/u,/game_content_revisions_published_select[\s\S]*status = 'published'/u])has(p,'public read ACL drift');return true;}
 if(id==='DATA-024'){for(const p of [/policy profiles_self_read[\s\S]*auth\.uid\(\)\) = id/u,/policy profiles_self_nickname_update[\s\S]*auth\.uid\(\)\) = id/u,/policy user_pets_self_read[\s\S]*auth\.uid\(\)\) = user_id/u,/grant select\(id,nickname,level,exp,gacha_points,created_at\), update\(nickname\) on public\.profiles to authenticated/u])has(p,'self ACL drift');return true;}
 if(id==='DATA-025'){for(const p of [/revoke all on public\.pet_catalog, public\.profiles, public\.user_pets, public\.matches, public\.match_players from public, anon, authenticated, service_role/u,/revoke all on all tables in schema private from public, anon, authenticated, service_role/u,/revoke execute on all functions in schema private from public, anon, authenticated, service_role/u])has(p,'authority denial drift');return true;}
 if(id==='DATA-026'){for(const p of [/security definer set search_path = pg_catalog/u,/owner to game_security_owner/u,/grant execute on function private\.join_match_participant_v1\(uuid,uuid,uuid\) to app_server/u])has(p,'trusted write boundary drift');return true;}
 if(id==='DATA-027'){const test=read('tests/database/concurrency.test.ts').replace(/\s+/gu,' ');if(!/Array\.from\(\{ length: 20 \}/u.test(test)||!/database concurrency tests only run against loopback/u.test(test)||!/allows exactly two seats across 20 real app_server sessions/u.test(test))throw Error('20-session evidence drift');return true;}
 if(id==='DATA-028'){const bodies=[...source.sql.matchAll(/create type public\.match_status as enum \(([^;]+)\)/gsu)],body=bodies.at(-1)?.[1],values=body?.match(/'[^']+'/gu)?.map(x=>x.slice(1,-1));if(JSON.stringify(values)!==JSON.stringify(MATCH_DB_PROJECTION_V1.phases))throw Error('domain enum parity drift');deriveTerminalConstraintProjection(source.sql);return true;}
 if(id==='DATA-029'){for(const p of [/create table private\.economy_policy_revisions/u,/create table private\.idempotency_requests/u,/create table private\.reward_ledger/u,/create table private\.pet_inventory/u,/create table private\.gacha_history/u,/create table private\.fusion_history/u,/create table private\.outbox_events/u,/function private\.award_match_reward_v1/u,/function private\.draw_pet_v1/u,/function private\.fuse_pets_v1/u,/to economy_server/u,/to economy_deployment_role/u])has(p,'economy persistence drift');return true;}
 throw Error('unsupported database requirement');}
export function evaluateSocketCommandRequirement(id:string){const m='00000000-0000-4000-8000-000000000002',r='00000000-0000-4000-8000-000000000001',payloads:Record<string,Record<string,unknown>>={'API-010':{type:'READY',contentRevisionId:m,contentHash:'a'.repeat(64),assetHashes:['b'.repeat(64),'c'.repeat(64)],decodedDimensions:[{assetHash:'b'.repeat(64),width:1,height:1},{assetHash:'c'.repeat(64),width:1,height:1}]},'API-011':{type:'TAP_IMAGE',imageSide:'A',x:.25,y:.75},'API-012':{type:'SUBMIT_FINAL_ANSWER',answer:'cat'},'API-013':{type:'SUBMIT_MEANING',optionId:'meaning_a'},'API-014':{type:'USE_HINT'}};const payload=payloads[id];if(!payload)throw Error('unsupported socket command requirement');const envelope={protocolVersion:1,requestId:r,matchId:m,expectedRevision:0,clientSeq:0,payload},parsed=clientCommandEnvelopeSchema.parse(envelope);if(parsed.payload.type!==payload.type)throw Error('command branch drift');for(const mutation of [{...envelope,protocolVersion:2},{...envelope,payload:{...payload,serverAuthority:true}}]){let rejected=false;try{clientCommandEnvelopeSchema.parse(mutation);}catch{rejected=true;}if(!rejected)throw Error('command mutation accepted');}return true;}
export function evaluateSocketSnapshotRequirement(id:string){if(id!=='API-015')throw Error('unsupported socket snapshot requirement');const state=structuredClone(createTestingReplayBundle().initialState),snapshot=projectSnapshot(state,'p1',1234),assets=snapshot.preload.assets.map(asset=>({...asset,url:`https://cdn.spot-learn.test/assets/${asset.sha256}.${asset.mimeType==='image/webp'?'webp':'png'}`})) as typeof snapshot.preload.assets,wire={...snapshot,preload:{...snapshot.preload,assets}};matchSnapshotV1Schema.parse(wire);if(wire.protocolVersion!==1||wire.stateRevision!==state.stateRevision||wire.lastEventSeq!==state.nextEventSeq-1)throw Error('snapshot version/cursor drift');if(JSON.stringify(wire.scores.map(x=>x.playerId))!==JSON.stringify(state.players.map(x=>x.playerId))||JSON.stringify(wire.connections.map(x=>x.playerId))!==JSON.stringify(state.connections.map(x=>x.playerId)))throw Error('snapshot ordering drift');const bytes=JSON.stringify(wire);for(const secret of ['privateSolution','canonicalAnswer','aliases','correctOptionId'])if(bytes.includes(secret))throw Error(`snapshot leaked ${secret}`);for(const mutation of [{...wire,privateSolution:{}},{...wire,protocolVersion:2},{...wire,finalChallenge:{...wire.finalChallenge,canonicalAnswer:'secret'}}])if(matchSnapshotV1Schema.safeParse(mutation).success)throw Error('snapshot mutation accepted');return true;}
export function evaluateSocketEventRequirement(id:string){const expected:Record<string,string>={'API-016':'match_started','API-017':'tap_result','API-018':'difference_claimed','API-019':'word_hunt_started','API-020':'word_hunt_won','API-021':'score_changed','API-022':'final_rush_started','API-023':'meaning_quiz_started','API-024':'match_finished'},type=expected[id];if(!type)throw Error('unsupported socket event requirement');const bundle=createTestingReplayBundle(),m=bundle.initialState.matchId,ready={type:'READY' as const,contentRevisionId:bundle.contentRevisionId,contentHash:bundle.contentHash,assetHashes:['a'.repeat(64),'c'.repeat(64)]as[string,string],decodedDimensions:[{assetHash:'a'.repeat(64),width:1,height:1},{assetHash:'c'.repeat(64),width:1,height:1}]as[{assetHash:string;width:number;height:number},{assetHash:string;width:number;height:number}]},pc=(state:MatchStateV1,playerId:string,seq:number,at:number,payload:object)=>{const requestId=`00000000-0000-4000-8000-${String(500+seq).padStart(12,'0')}`;return{source:'PLAYER' as const,commandId:`${m}:player:${playerId}:${requestId}`,matchId:m,commandSeq:seq,receivedAtMs:at,requestId,playerId,expectedRevision:state.stateRevision,payload}};let state=structuredClone(bundle.initialState),events:Array<ReturnType<typeof reduceMatch>['events'][number]>=[];for(const [i,p]of['p1','p2'].entries()){const result=reduceMatch(state,pc(state,p,i+1,1000,ready),testingRules);state=result.state;}const startId=`${m}:timer:START_MATCH:countdown`,started=reduceMatch(state,{source:'TIMER',commandId:startId,timerId:startId,matchId:m,commandSeq:3,receivedAtMs:4000,dueAtMs:4000,payload:{type:'START_MATCH'}},testingRules);state=started.state;events.push(...started.events);const tap=reduceMatch(state,pc(state,'p1',4,5000,{type:'TAP_IMAGE',imageSide:'A',x:.1,y:.2}),testingRules);state=tap.state;events.push(...tap.events);const whId=`${m}:timer:START_WORD_HUNT:w1`,wh=reduceMatch(state,{source:'TIMER',commandId:whId,timerId:whId,matchId:m,commandSeq:5,receivedAtMs:20000,dueAtMs:20000,payload:{type:'START_WORD_HUNT',missionId:'w1'}},testingRules);state=wh.state;events.push(...wh.events);const won=reduceMatch(state,pc(state,'p1',6,21200,{type:'TAP_IMAGE',imageSide:'A',x:.2,y:.8}),testingRules);state=won.state;events.push(...won.events);const rushId=`${m}:timer:START_FINAL_RUSH:final-rush`,rush=reduceMatch(state,{source:'TIMER',commandId:rushId,timerId:rushId,matchId:m,commandSeq:7,receivedAtMs:64000,dueAtMs:64000,payload:{type:'START_FINAL_RUSH'}},testingRules);state=rush.state;events.push(...rush.events);state.activeMission=null;state.finalChallenge={unlockedAtMs:64000,unlockSource:'TIME'};const answer=reduceMatch(state,pc(state,'p1',8,65000,{type:'SUBMIT_FINAL_ANSWER',answer:'cat'}),testingRules);state=answer.state;events.push(...answer.events);state.players[0]!.score=1;const closeId=`${m}:timer:CLOSE_INPUT:close`,finish=reduceMatch(state,{source:'TIMER',commandId:closeId,timerId:closeId,matchId:m,commandSeq:9,receivedAtMs:79000,dueAtMs:79000,payload:{type:'CLOSE_INPUT'}},testingRules);state=finish.state;events.push(...finish.events);const internal=events.find(e=>({match_started:'MATCH_STARTED',tap_result:'TAP_RESOLVED',difference_claimed:'OBJECTIVE_CLAIMED',word_hunt_started:'WORD_HUNT_STARTED',word_hunt_won:'WORD_HUNT_WON',score_changed:'SCORE_CHANGED',final_rush_started:'FINAL_RUSH_STARTED',meaning_quiz_started:'MEANING_QUIZ_STARTED',match_finished:'MATCH_FINISHED'}as Record<string,string>)[type]===e.type);if(!internal)throw Error(`reducer event missing ${type}`);const wire=projectMatchEvent(internal,state,'p1');serverEventEnvelopeSchema.parse(wire);if(wire.type!==type||wire.protocolVersion!==1||wire.eventSeq!==internal.eventSeq||wire.stateRevision!==internal.stateRevision)throw Error('event projection cursor drift');for(const mutation of [{...wire,protocolVersion:2},{...wire,eventSeq:0},{...wire,payload:{...wire.payload,canonicalAnswer:'secret'}},{...wire,privateSolution:{}}])if(serverEventEnvelopeSchema.safeParse(mutation).success)throw Error('event mutation accepted');if(type==='tap_result'){const hidden=projectMatchEvent(internal,state,'p2');serverEventEnvelopeSchema.parse(hidden);if(hidden.type!=='state_advanced'||hidden.eventSeq!==wire.eventSeq||hidden.stateRevision!==wire.stateRevision)throw Error('private viewer cursor redaction drift');}return true;}
export function evaluateSocketFinishedRequirement(id:string){if(id!=='API-024')throw Error('unsupported finished event requirement');const state=structuredClone(createTestingReplayBundle().initialState),m=state.matchId,commandId=`${m}:system:no-contest:api024`,result=reduceMatch(state,{source:'SYSTEM',commandId,systemCommandId:commandId,matchId:m,commandSeq:1,receivedAtMs:1,payload:{type:'CANCEL_NO_CONTEST',incidentId:'api024',reason:'SERVER_OWNERSHIP_LOST'}},testingRules),event=result.events.find(x=>x.type==='MATCH_FINISHED');if(!event)throw Error('reducer match_finished missing');const wire=projectMatchEvent(event,result.state,'p1');serverEventEnvelopeSchema.parse(wire);if(wire.type!=='match_finished'||wire.eventSeq!==event.eventSeq||wire.stateRevision!==event.stateRevision)throw Error('finished cursor drift');for(const mutation of [{...wire,protocolVersion:2},{...wire,eventSeq:0},{...wire,payload:{...wire.payload,canonicalAnswer:'secret'}}])if(serverEventEnvelopeSchema.safeParse(mutation).success)throw Error('finished mutation accepted');return true;}
export function evaluateApiWireComposite(id:string,provided?:string){if(id!=='API-005')throw Error('unsupported API composite requirement');const source=provided??fs.readFileSync(path.resolve('packages/contracts/openapi.yaml'),'utf8');for(const restId of Object.keys(REST_REQUIREMENTS))evaluateOpenApiRequirement(restId,source);const mustReject=(mutated:string,restId:string)=>{let rejected=false;try{evaluateOpenApiRequirement(restId,mutated);}catch{rejected=true;}if(!rejected)throw Error('REST security mutation accepted');};mustReject(source.replace('security: [{ bearerAuth: [] }]','security: []'),'API-001');mustReject(source.replaceAll('{ $ref: "#/components/parameters/IdempotencyKey" }','{ $ref: "#/components/parameters/Id" }'),'API-003');for(const commandId of ['API-010','API-011','API-012','API-013','API-014'])evaluateSocketCommandRequirement(commandId);evaluateSocketSnapshotRequirement('API-015');for(let n=16;n<=23;n++)evaluateSocketEventRequirement(`API-${String(n).padStart(3,'0')}`);evaluateSocketFinishedRequirement('API-024');const ack={protocolVersion:1,requestId:'00000000-0000-4000-8000-000000000001',accepted:false,reason:'INVALID_PAYLOAD',stateRevision:2,lastEventSeq:3,snapshotRequired:false};commandAckSchema.parse(ack);for(const mutation of [{...ack,privateSolution:{}},{...ack,failureDetail:'secret'},{...ack,protocolVersion:2}])if(commandAckSchema.safeParse(mutation).success)throw Error('rejected ack leak accepted');return true;}
export function evaluateContentSchemaRequirement(id:string){const schemas={public:publicGameContentSchema,private:privateGameSolutionSchema,rights:rightsManifestSetSchema},keys=(value:{required?:readonly string[]})=>[...(value.required??[])].sort(),exact=(a:unknown,b:unknown,label:string)=>{if(JSON.stringify(a)!==JSON.stringify(b))throw Error(`${label} drift`);};if(id==='CONTENT-001'){exact(Object.values(schemas).map(x=>[x.title,x.additionalProperties]),[['PublicGameContentV1',false],['PrivateGameSolutionV1',false],['RightsManifestSetV1',false]],'bundle separation');return true;}if(id==='CONTENT-002'){exact(keys(schemas.public),['assetPolicyVersion','contentId','contentRevisionId','difficulty','imageA','imageB','language','schemaVersion','theme','version'].sort(),'public fields');return true;}if(id==='CONTENT-003'){exact(keys(schemas.private),['contentRevisionId','differences','finalChallenge','privateSolutionHash','schemaVersion','suddenDeath','wordHunts'].sort(),'private fields');return true;}if(id==='CONTENT-004'){const entry=(schemas.rights.properties.entries.items as {required:readonly string[]});exact(keys(entry),['assetSha256','education','generator','prompt','rights','rightsRecordId','source','takedown'].sort(),'rights fields');return true;}if(id==='CONTENT-005'){for(const [name,schema]of Object.entries(schemas)){const generated=JSON.parse(fs.readFileSync(path.resolve(name==='public'?'schemas/game-content.public.schema.json':name==='private'?'schemas/game-content.private.schema.json':'schemas/rights-manifest.schema.json'),'utf8'));exact(generated,schema,`${name} generated schema`);}return true;}const files:Record<string,[string,unknown]>={'CONTENT-007':['schemas/game-content.public.schema.json',schemas.public],'CONTENT-008':['schemas/game-content.private.schema.json',schemas.private],'CONTENT-009':['schemas/rights-manifest.schema.json',schemas.rights]};if(id==='CONTENT-006'){const legacy=JSON.parse(fs.readFileSync(path.resolve('schemas/game-content.schema.json'),'utf8'));if(legacy.title==='PublicGameContentV1'||legacy.title==='PrivateGameSolutionV1'||legacy.title==='RightsManifestSetV1')throw Error('legacy contract reused');return true;}const pair=files[id];if(pair){exact(JSON.parse(fs.readFileSync(path.resolve(pair[0]),'utf8')),pair[1],id);return true;}throw Error('unsupported content schema requirement');}
export function evaluateContentSemanticRequirement(id:string){if(id==='CONTENT-010'){const requestId='00000000-0000-4000-8000-000000000010',matchId='00000000-0000-4000-8000-000000000011',wire=(answer:string)=>clientCommandEnvelopeSchema.safeParse({protocolVersion:1,requestId,matchId,expectedRevision:0,clientSeq:0,payload:{type:'SUBMIT_FINAL_ANSWER',answer}}).success,reducer=(answer:string)=>parseMatchCommandV1({source:'PLAYER',commandId:`${matchId}:player:p1:${requestId}`,matchId,commandSeq:1,receivedAtMs:1,requestId,playerId:'p1',expectedRevision:0,payload:{type:'SUBMIT_FINAL_ANSWER',answer}});const exactCodePoints='a'.repeat(CONTENT_TEXT_LIMITS_V1.maxCodePoints),exactBytes='😀'.repeat(CONTENT_TEXT_LIMITS_V1.maxCodePoints),offByOne='a'.repeat(CONTENT_TEXT_LIMITS_V1.maxCodePoints+1);if(!isWithinContentTextLimits(exactCodePoints)||!isWithinContentTextLimits(exactBytes)||isWithinContentTextLimits(offByOne)||!wire(exactCodePoints)||!wire(exactBytes)||wire(offByOne))throw Error('wire content text limit drift');reducer(exactCodePoints);reducer(exactBytes);let rejected=false;try{reducer(offByOne);}catch{rejected=true;}if(!rejected)throw Error('reducer content text limit drift');return true;}const cases=CONTENT_REQUIREMENT_CASES[id as keyof typeof CONTENT_REQUIREMENT_CASES];if(!cases?.length)throw Error('unsupported content semantic requirement');if(new Set(cases.map(x=>`${x[0]}:${x[1]}`)).size!==cases.length)throw Error('duplicate semantic evidence');return true;}
export function evaluatePureGameRequirement(id:string){
 const bundle=createTestingReplayBundle(),state:MatchStateV1=structuredClone(bundle.initialState),m=state.matchId;
 if(['STATE-002','STATE-003','STATE-004','STATE-005'].includes(id)){state.phase='PLAYING';state.startedAtMs=0;state.gameplayClosesAtMs=testingRules.time.playingMs;state.settlementCapAtMs=testingRules.time.meaningSettlementCapMs;state.finalChallenge={unlockedAtMs:1,unlockSource:'TIME'};const p=state.players[0]!;if(id==='STATE-003')p.answerUntilMs=100;if(id==='STATE-004')p.tapRateWindow={windowIndex:0,acceptedCount:testingRules.limits.maxBoardTapsPerSecond};if(id==='STATE-005'){p.finalAnswerStatus='MEANING_PENDING';state.meaningQuizzes=[{playerId:p.playerId,quizOrdinal:1,startedAtMs:1,endsAtMs:100,submitted:false}];}const actual=derivePlayerInputState(state,p.playerId,50,testingRules,'CONNECTED'),expected=id==='STATE-002'?{board:'ENABLED',answer:'ENABLED',overlay:'NONE'}:id==='STATE-003'?{board:'ENABLED',answer:'LOCKED',overlay:'NONE'}:id==='STATE-004'?{board:'RATE_LIMITED',answer:'ENABLED',overlay:'NONE'}:{board:'DISABLED',answer:'LOCKED',overlay:'MEANING_QUIZ'};if(JSON.stringify(actual)!==JSON.stringify(expected))throw Error(`${id} input projection drift`);return true;}
 if(id==='STATE-006'){if(terminalPhaseForEndReason('SCORE_TARGET')!=='FINISHED'||terminalPhaseForEndReason('NO_CONTEST')!=='CANCELLED')throw Error('terminal mapping drift');return true;}
 if(id==='STATE-007'){parseMatchStateV1(state);const mutated=structuredClone(state);mutated.privateSolution.differences[0]!.hitboxes.imageA.cx=1.000001;let rejected=false;try{parseMatchStateV1(mutated);}catch{rejected=true;}if(!rejected)throw Error('out-of-range hitbox accepted');return true;}
 if(id==='STATE-008'){const rect={x:20,y:30,width:400,height:200},transform={scale:2,tx:11,ty:-7},point={x:.25,y:.75},roundTrip=inverseContentPoint(contentToScreen(point,rect,transform),rect,transform);if(Math.abs(roundTrip.x-point.x)>1e-12||Math.abs(roundTrip.y-point.y)>1e-12)throw Error('coordinate inverse drift');return true;}
 if(id==='STATE-010'){const reconnecting=derivePlayerInputState(state,'p1',0,testingRules,'RECONNECTING');if(reconnecting.overlay!=='RECONNECTING'||reconnecting.board!=='DISABLED'||reconnecting.answer!=='LOCKED'||state.phase!=='WAITING_FOR_ASSETS'||reconnectOverlayForElapsed(4999)!=='RECONNECTING'||reconnectOverlayForElapsed(5000)!=='OFFLINE')throw Error('transport overlay leaked into match phase');return true;}
 if(id==='STATE-001'){replayMatch(bundle);if(testingRules.time.playingMs!==75000||testingRules.time.meaningSettlementCapMs!==80000)throw Error('time boundary drift');let rejected=false;try{replayMatch({...bundle,rulesetHash:'0'.repeat(64)});}catch{rejected=true;}if(!rejected)throw Error('boundary mutation accepted');return true;}
 if(id==='STATE-009'){const cancelled=reduceMatch(state,{source:'SYSTEM',commandId:`${m}:system:no-contest:oracle`,systemCommandId:`${m}:system:no-contest:oracle`,matchId:m,commandSeq:1,receivedAtMs:1,payload:{type:'CANCEL_NO_CONTEST',incidentId:'oracle',reason:'SERVER_OWNERSHIP_LOST'}},testingRules);if(cancelled.state.phase!=='CANCELLED'||cancelled.state.endReason!=='NO_CONTEST')throw Error('terminal transition');return true;}
 if(id==='OBS-012'){const requestId='00000000-0000-4000-8000-000000000991';let rejected=false;try{replayMatch({...bundle,commands:[{source:'PLAYER',commandId:`${m}:player:p1:${requestId}`,matchId:m,commandSeq:2,receivedAtMs:1,requestId,playerId:'p1',expectedRevision:0,payload:{type:'USE_HINT'}}]});}catch{rejected=true;}if(!rejected)throw Error('sequence gap accepted');return true;}
 if(id==='OBS-013'){const disconnectId=`${m}:system:connection:p1:1:DISCONNECTED`;const disconnected=reduceMatch(state,{source:'SYSTEM',commandId:disconnectId,systemCommandId:disconnectId,matchId:m,commandSeq:1,receivedAtMs:10000,payload:{type:'PLAYER_CONNECTION_CHANGED',playerId:'p1',disconnectEpoch:1,status:'DISCONNECTED'}},testingRules);const timerId=`${m}:timer:DISCONNECT_FORFEIT_TIMEOUT:p1:1`;const ended=reduceMatch(disconnected.state,{source:'TIMER',commandId:timerId,timerId,matchId:m,commandSeq:2,receivedAtMs:25000,dueAtMs:25000,payload:{type:'DISCONNECT_FORFEIT_TIMEOUT',playerId:'p1',disconnectEpoch:1}},testingRules);if(ended.state.endReason!=='FORFEIT'||ended.state.winnerPlayerId!=='p2')throw Error('deadline timer did not win');return true;}
 if(id==='OBS-011'){const commandId=`${m}:system:no-contest:oracle`;const ended=reduceMatch(state,{source:'SYSTEM',commandId,systemCommandId:commandId,matchId:m,commandSeq:1,receivedAtMs:1,payload:{type:'CANCEL_NO_CONTEST',incidentId:'oracle',reason:'BOTH_DISCONNECTED'}},testingRules);if(ended.state.phase!=='CANCELLED'||ended.state.endReason!=='NO_CONTEST'||ended.state.winnerPlayerId!==null)throw Error('both players lost');return true;}
 if(id==='OBS-001'){const member=resolveAuthenticatedParticipant('auth-subject',[{authSubject:'auth-subject',matchId:m,participantKey:'p1'}],m,'00000000-0000-4000-8000-000000000991');if(member!=='p1'||decideDelivery({lastEventSeq:1,stateRevision:1},{kind:'EVENT',eventSeq:3,stateRevision:2})!=='REQUEST_REPLAY'||decideDelivery({lastEventSeq:1,stateRevision:1},{kind:'REPLAY_UNAVAILABLE'})!=='REPLACE_SNAPSHOT'||decideReconnect(14999)!=='RESUME'||decideReconnect(15000)!=='FORFEIT')throw Error('recovery authority drift');return true;}
 if(id==='OBS-003'){for(const key of ['score','correctAnswer','winnerPlayerId','activeMission','buff','answerUntilMs']){let accepted=true;try{clientCommandEnvelopeSchema.parse({protocolVersion:1,requestId:'00000000-0000-4000-8000-000000000991',matchId:m,expectedRevision:0,clientSeq:0,payload:{type:'USE_HINT',[key]:1}});}catch{accepted=false;}if(accepted)throw Error(`client authority accepted: ${key}`);}return true;}
 if(id==='OBS-005'){state.phase='PLAYING';state.startedAtMs=0;state.gameplayClosesAtMs=testingRules.time.playingMs;state.settlementCapAtMs=testingRules.time.meaningSettlementCapMs;const tap=(x:number,seq:number)=>{const requestId=`00000000-0000-4000-8000-${String(990+seq).padStart(12,'0')}`;return reduceMatch(state,{source:'PLAYER',commandId:`${m}:player:p1:${requestId}`,matchId:m,commandSeq:seq,receivedAtMs:1000,requestId,playerId:'p1',expectedRevision:0,payload:{type:'TAP_IMAGE',imageSide:'A',x,y:.2}},testingRules)};if(!tap(.1,1).events.some(e=>e.type==='TAP_RESOLVED'&&e.payload.hit===true)||!tap(.13,2).events.some(e=>e.type==='TAP_RESOLVED'&&e.payload.hit===false))throw Error('coordinate validation drift');return true;}
 if(id==='OBS-008'){let accepted=true;try{clientCommandEnvelopeSchema.parse({protocolVersion:1,requestId:'00000000-0000-4000-8000-000000000991',matchId:m,expectedRevision:0,clientSeq:0,payload:{type:'TAP_IMAGE',imageSide:'A',x:.1,y:.2,score:999}});}catch{accepted=false;}if(accepted)throw Error('client score accepted');return true;}
 if(id==='OBS-009'){if(reconnectOverlayForElapsed(4999)!=='RECONNECTING'||reconnectOverlayForElapsed(5000)!=='OFFLINE'||decideReconnect(14999)!=='RESUME'||decideReconnect(15000)!=='FORFEIT')throw Error('reconnect boundaries drift');return true;}
 throw Error('unsupported pure game requirement');
}
const readyPayload={type:'READY' as const,contentRevisionId:'00000000-0000-4000-8000-000000000010',contentHash:'d'.repeat(64),assetHashes:['a'.repeat(64),'c'.repeat(64)]as[string,string],decodedDimensions:[{assetHash:'a'.repeat(64),width:1,height:1},{assetHash:'c'.repeat(64),width:1,height:1}]as[{assetHash:string;width:number;height:number},{assetHash:string;width:number;height:number}]};
export function evaluateSecurityRequirement(id:string){const bundle=createTestingReplayBundle(),state:MatchStateV1=structuredClone(bundle.initialState),m=state.matchId;const command=(playerId:string,seq:number)=>{const requestId=`00000000-0000-4000-8000-${String(990+seq).padStart(12,'0')}`;return{source:'PLAYER' as const,commandId:`${m}:player:${playerId}:${requestId}`,matchId:m,commandSeq:seq,receivedAtMs:1000,requestId,playerId,expectedRevision:state.stateRevision,payload:readyPayload};};
 if(id==='SEC-001'){const pinned={protocolVersion:1 as const,engineVersion:'1',rulesetVersion:'1.0.0' as const,rulesetHash:'a'.repeat(64),contentRevisionId:readyPayload.contentRevisionId,contentHash:'d'.repeat(64)};if(negotiateCompatibility({protocolVersion:1,supportedEngineVersions:['2'],supportedRulesetVersions:['1.0.0']},pinned).accepted!==false)throw Error('update admission');clientCommandEnvelopeSchema.parse({protocolVersion:1,requestId:'00000000-0000-4000-8000-000000000991',matchId:m,expectedRevision:0,clientSeq:0,payload:{type:'USE_HINT'}});if(decideDelivery({lastEventSeq:2,stateRevision:2},{kind:'EVENT',eventSeq:4,stateRevision:3})!=='REQUEST_REPLAY')throw Error('journal gap');return true;}
 if(id==='SEC-002'){const pkg=JSON.parse(fs.readFileSync(path.resolve('apps/mobile/package.json'),'utf8'));if(pkg.dependencies.expo!=='57.0.1'||pkg.dependencies['react-native']!=='0.86.0')throw Error('mobile stack drift');const snapshot=projectSnapshot(state,'p1',0),vm=adaptMatchSnapshot(snapshot,{pendingIntentId:'pending',connection:'CONNECTED'});if(createTapIntent(vm,{side:'A',x:.1,y:.2})!==null||JSON.stringify(snapshot).includes('privateSolution'))throw Error('public fail closed');return true;}
 if(id==='SEC-004'){let accepted=true;try{clientCommandEnvelopeSchema.parse({protocolVersion:1,requestId:'00000000-0000-4000-8000-000000000991',matchId:m,expectedRevision:0,clientSeq:0,payload:{type:'TAP_IMAGE',imageSide:'A',x:.1,y:.2,score:1}});}catch{accepted=false;}if(accepted)throw Error('client score authority');return true;}
 if(id==='SEC-005'){const snapshot=projectSnapshot(state,'p1',0);if(state.startedAtMs!==null||snapshot.preload.assets.length!==2||snapshot.phase!=='WAITING_FOR_ASSETS')throw Error('preload ordering');return true;}
 if(id==='SEC-006'){if(RECOMMENDED_IMAGE_LONG_EDGE_PX!==1536)throw Error('asset recommendation drift');return true;}
 if(id==='SEC-007'){const first=reduceMatch(state,command('p1',1),testingRules);if(first.state.phase!=='WAITING_FOR_ASSETS')throw Error('single ack started');const secondCommand=command('p2',2);secondCommand.expectedRevision=first.state.stateRevision;const second=reduceMatch(first.state,secondCommand,testingRules);if(second.state.phase!=='COUNTDOWN')throw Error('both ack did not start');return true;}
 throw Error('unsupported security requirement');}
export function evaluateAnalyticsRequirement(id:string){const base={eventVersion:1,eventSeq:1,stateRevision:0,occurredAt:'2026-07-19T00:00:00.000Z',matchId:'match_opaque',anonymousUserId:'anon_rotating',traceId:'trace_opaque',engineVersion:'1.0.0',rulesetVersion:'1.0.0',contentRevisionId:'content_1',experimentVariant:'CONTROL',serverVersion:'server_1',protocolVersion:1};if(id==='OBS-006'){parseAnalyticsEventV1({...base,name:'same_coordinate_burst_signal',data:{cellBucket:'03:12',countBucket:'8-15',windowDurationBucket:'1-2s'}});let rejected=false;try{parseAnalyticsEventV1({...base,name:'same_coordinate_burst_signal',data:{x:.5,y:.5,timestamp:1}});}catch{rejected=true;}if(!rejected)throw Error('raw coordinates accepted');return true;}if(id==='OBS-007'){parseAnalyticsEventV1({...base,name:'answer_reaction_time_signal',data:{durationBucket:'250-499ms'}});return true;}if(id==='OBS-014'){const stages=['queue','handshake','preload','command','finish','reward']as const,rows=stages.map((stage,i)=>({stage,eventSeq:i+1,traceId:'trace_opaque_x',matchId:'match_opaque_x',requestId:`request_opaque_${i}`,...(stage==='reward'?{effectId:'effect_opaque_x'}:{})}));validateLifecycleTrace(rows);const collector=new AnalyticsCollector();collector.recordRequest({requestId:'r',outcome:'UNEXPECTED_FAILURE',reason:'ACK_TIMEOUT',occurredAtMs:1});collector.recordRequest({requestId:'r',outcome:'SUCCESS',occurredAtMs:2});if(collector.snapshot().uniqueSchemaValidRequests!==1||collector.snapshot().unexpectedFailures!==0)throw Error('terminal retry grouping');return true;}if(id==='OBS-015'){const samples=Array.from({length:10000},(_,i)=>({requestId:`r${i}`,outcome:'SUCCESS'as const,occurredAtMs:i*100,durationMs:1,concurrency:100,schemaValid:true,duplicateClaim:false,duplicateReward:false,finishLost:false})),result=evaluateLoadSamples(samples,{soakStartMs:0,soakEndMs:1800000,expectedConcurrency:100});if(result.full30m.denominator!==10000||result.evidenceScope!=='DETERMINISTIC_LOCAL_NOT_PRODUCTION')throw Error('load evidence');return true;}throw Error('unsupported analytics requirement');}
export function executeRequirementOracle(root:string,row:Row,claim:Claim){
 let status:RequirementGateResult='FAIL';
 try{
  const line=fs.readFileSync(path.join(root,row.source),'utf8').split(/\r?\n/u)[row.sourceLine-1]??'';
  if(!line.includes(`<!-- REQ: ${row.id} -->`)||!line.includes(row.text))throw Error('source projection mismatch');
  if(claim.lifecycle&&claim.lifecycle!=='CURRENT'){
   if(!validateNonCurrentEvidence(root,claim))throw Error('invalid lifecycle closure evidence');status='BLOCKED';
  }else switch(claim.oracle.kind){
   case'STATE_SCHEMA':evaluatePureGameRequirement(row.id);status='PASS';break;
   case'PRIVACY_CONTRACT':evaluateSecurityRequirement(row.id);status='PASS';break;
   case'ANALYTICS_CONTRACT':if(['OBS-001','OBS-003','OBS-005','OBS-008','OBS-009','OBS-011','OBS-012','OBS-013'].includes(row.id))evaluatePureGameRequirement(row.id);else evaluateAnalyticsRequirement(row.id);status='PASS';break;
   case'RULESET_PARSE':{const value=JSON.parse(fs.readFileSync(path.join(root,claim.oracle.input??'config/ruleset.v1.json'),'utf8'));parseRuleset(value);assertExact(value,claim.oracle.assertions);status='PASS';break;}
   case'ECONOMY_PARSE':{const value=JSON.parse(fs.readFileSync(path.join(root,claim.oracle.input??'config/economy.v1.json'),'utf8'));parseEconomy(value);assertExact(value,claim.oracle.assertions);status='PASS';break;}
   case'UI_CONTRACT':{const value=JSON.parse(fs.readFileSync(path.join(root,claim.oracle.input??'config/ui-screen-contract.v1.json'),'utf8')),load=(file:string)=>JSON.parse(fs.readFileSync(path.join(root,file),'utf8')),bundle={theme:load('config/ui-theme.v1.json'),screens:value,references:load('docs/design/ui-reference/manifest.json'),rights:load('docs/design/ui-reference/rights-manifest.json')};if(validateUiReferenceBundle(bundle).length)throw Error('UI bundle invalid');assertExact(value,claim.oracle.assertions);status='PASS';break;}
   case'DB_PROJECTION':evaluateDatabaseRequirement(row.id);status='PASS';break;
   case'OPENAPI_PROJECTION':if(row.id==='API-005')evaluateApiWireComposite(row.id);else if(/^API-01[0-4]$/u.test(row.id))evaluateSocketCommandRequirement(row.id);else if(row.id==='API-015')evaluateSocketSnapshotRequirement(row.id);else if(row.id==='API-024')evaluateSocketFinishedRequirement(row.id);else if(/^API-0(?:1[6-9]|2[0-3])$/u.test(row.id))evaluateSocketEventRequirement(row.id);else evaluateOpenApiRequirement(row.id);status='PASS';break;
   case'CONTENT_SCHEMA':if(/^CONTENT-00[1-9]$/u.test(row.id))evaluateContentSchemaRequirement(row.id);else evaluateContentSemanticRequirement(row.id);status='PASS';break;
   default:throw Error('missing current executable oracle');
  }
 }catch{status='FAIL';}
 return{status,metric:emitRequirementGateStatus(row.id,status)};
}
