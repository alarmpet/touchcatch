# iOS guest-device evidence

Status: `BLOCKED`

The current Windows host cannot build an iOS development client and no
physical iPhone/macOS build host is connected. No Android, web, simulator, or
synthetic result was substituted for iOS evidence.
